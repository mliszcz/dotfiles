[user]
    name = mliszcz
    email = liszcz.michal@gmail.com
    signingkey = ABA8CF69F7A2925FB4330C2239ADAE5339E2BAD0

[core]
    pager = "type delta >/dev/null 2>&1 && exec delta || exec less -FRX"

[merge]
    conflictstyle = diff3

[push]
    gpgSign = if-asked

[branch]
    sort = committerdate

[diff]
    colorMoved = default
    interHunkContext = 20
    algorithm = patience

[delta]
    navigate = true  # use n and N to move between diff sections
    line-numbers = true
    blame-format = "{timestamp:<18} {author:<15.14} {commit:<8} "
    blame-timestamp-output-format = "%Y-%m-%d %H:%M"
